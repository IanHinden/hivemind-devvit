DEVVIT_SUBREDDIT=r/my_subreddit

# Optional: secret for admin-only endpoints (GET /api/next-subreddit, GET /api/reported-posts).
# Set ADMIN_SECRET to a long random string; pass it as ?key=YOUR_SECRET when calling those endpoints.
# Example: ADMIN_SECRET=your-long-random-string-here

# Optional: port for the server when running locally (if your dev setup uses it).
# Example: PORT=8080

# Optional: override daily subreddit for testing (e.g. videos to test video support).
# Example: TEST_SUBREDDIT=videos

# --- Local access to admin endpoints (e.g. Postman) ---
# 1. Copy this file to .env and set ADMIN_SECRET (and PORT if needed).
# 2. Start the app: npm run dev (or devvit playtest). Note the port the server logs (e.g. 8080).
# 3. In Postman or browser:
#    GET http://localhost:PORT/api/reported-posts?key=YOUR_ADMIN_SECRET
#    GET http://localhost:PORT/api/next-subreddit?key=YOUR_ADMIN_SECRET&days=7